[package]
name = "lazydesktop-watcher"
version = "0.1.0"
edition = "2021"
description = "LazyDesktop file watcher: typed VCS-aware filesystem events with debouncing."

[lib]
crate-type = ["staticlib"]
name = "watcher"

[dependencies]
notify = "7"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
