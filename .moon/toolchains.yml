# Moon toolchain configuration
# https://moonrepo.dev/docs/config/toolchain

rust:
  version: 'stable'
  bins:
    - 'cargo'
  # clippy and rustfmt are rustup components, not standalone binaries
  # They ship with the stable toolchain automatically
