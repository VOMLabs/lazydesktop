# ── Build artifacts ────────────────────────────────────────────────
build/
target/
.xmake/
xmake.lock
compile_commands.json

# ── Packaging output (AppImage, .deb, Arch, MSI) ───────────────────
AppDir/
*.AppImage
*.deb
*.pkg.tar.zst
*.msi

# ── Editor / IDE ───────────────────────────────────────────────────
.vscode/
!.vscode/settings.json
.idea/
*.swp
*~
.DS_Store
.cache/
.clangd/

# ── Temporary / session files ──────────────────────────────────────
.tmp/
.moon/cache/
*.log

# ── Environment / secrets ──────────────────────────────────────────
.env
.env.*
!.env.example

# ── Local tooling ──────────────────────────────────────────────────
.mise.local.toml